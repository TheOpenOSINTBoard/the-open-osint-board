# The Open OSINT Board — Backend Dependencies
# Supports: Windows 10, Windows 11, macOS, Linux
# Requires: Python 3.8 or higher

# Core web server
flask>=3.0.0,<4.0.0

# Cross-origin headers (lets the browser HTML file talk to localhost:5000)
flask-cors>=3.0.10

# HTTP client for all OSINT API calls (bundles SSL certs — no system config needed)
requests>=2.28.0

# Feed refresh scheduler
apscheduler>=3.9.0,<4.0.0

# SSL certificates (pinned explicitly for reliability on Windows)
certifi>=2023.0.0

# Timezone support (needed on Windows — Python's zoneinfo needs tzdata on Win)
tzdata>=2023.1
